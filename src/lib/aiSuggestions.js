// Función de IA sacada de la app por pedido explícito — este archivo ya no
// se usa en ningún lado (ModGenerador.jsx ya no lo importa). Queda vacío
// para que el próximo commit lo borre del todo (ver instrucciones en el
// chat: hay que hacer `git rm` porque el sandbox no puede eliminar
// archivos directamente).
